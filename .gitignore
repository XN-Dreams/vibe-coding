node_modules/
.DS_Store
Thumbs.db
*.log
.env
.env.*
!.env.example

# extract-design-system working cache
.extract-design-system/

# Operator session handoffs. Deliberately NOT committed: they reference paths
# outside this repo, and CLAUDE.md seals this public repo from everything else
# on the machine. The public record of a session is episodes/ep-NN.md.
docs/notes/
